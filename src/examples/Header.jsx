import React from 'react';
import { Button } from '../Button';
const Header = () => {
  return (
    <div>
      <h1>header component</h1>
      <Button>from header component</Button>
    </div>
  );
};

export default Header;
